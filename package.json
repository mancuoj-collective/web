{
  "scripts": {
    "dev": "bun index.html --hot --console",
    "build": "bun build index.html --minify --outdir=dist",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "@mancuoj/prettier-config": "^0.5.3",
    "prettier": "^3.6.2"
  },
  "prettier": "@mancuoj/prettier-config"
}
